const { LogError } = require("../../../helpers");

class CrudUserController {
  async store(req, res) {
    try {
    } catch (e) {
      return LogError(__dirname + __filename, e);
    }
  }

  async show(req, res) {
    try {
    } catch (e) {
      return LogError(__dirname + __filename, e);
    }
  }

  async update(req, res) {
    try {
    } catch (e) {
      return LogError(__dirname + __filename, e);
    }
  }
}

module.exports = new CrudUserController();
